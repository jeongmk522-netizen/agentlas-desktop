// Work now mounts the canonical OneActivityArtifactRail directly from
// TaskCockpit. Keep this compatibility module empty so stale imports cannot
// reintroduce the removed Work-only rail or its duplicate browser tabs.
export type ChatRightPanelTab = "agent" | "file" | "panel" | "memory";

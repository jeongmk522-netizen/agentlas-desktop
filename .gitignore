# dependencies
node_modules/
.pnp
.pnp.js

# build outputs (root-anchored — must NOT swallow app routes like
# renderer/app/(shell)/build/ which is real source, not build output)
dist/
/out/
/build/
.next/
renderer/.next/
renderer/.next-build/
renderer/out/
*.tsbuildinfo

# Electron-builder
release/
*.dmg
*.app
*.exe
*.AppImage

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# local env
.env
.env.local
.env.*.local

# editor / OS
.DS_Store
._*
.vscode/
.idea/
*.swp

# local db / cache
*.sqlite
*.sqlite-journal
.cache/

# code-sign secrets
build-resources/notarize-creds.json
signing/*
!signing/
!signing/README.md
credentials/*
!credentials/
!credentials/README.md
release-signing/
*.p12
*.key
*.csr
*.pem
*.p8
*.mobileprovision
*.jks
*.keystore

# Agentlas architecture: ignore volatile runtime memory, keep curated soul/sitemap
.agentlas/memory-log.jsonl
# Generated AI code-map (produced by the code-map tool background gen — not source)
.agentlas/code-map/
._*

# local packaging output (large)
release-local/
# large demo/verification outputs
artifacts/
evidence/
# generated Playwright QA screenshots/reports
output/playwright/
# Local visual research can contain browser chrome or account-identifying text.
/output/reference-video-frames/
/output/actual-live-ontology/
/output/experience-chip-management-ko.png
/findings/

# Internal working documents are local-only. New docs, plans, PRDs, research,
# QA receipts, and private screenshots must never enter a release commit.
/docs/**
# 예외 — 제품 계약 문서. 플러그인 규격은 데스크탑·터미널·허브가 공유하는 정본이라
# 릴리스 입력이다. 내부 작업 문서가 아니므로 /docs/** 에서 명시적으로 되살린다.
!/docs/PLUGIN-SPEC.md

# Other conventional local-only document roots and root-level PM receipts.
/internal/
/.internal/
/private-docs/
/plans/
/research/
/reports/
/receipts/
/notes/
/qa/
/*_INTERNAL*.md
/*_LEDGER*.md
/*_RECEIPT*.md
/*_RESEARCH*.md
/*_PLAN*.md
/*_NOTES*.md

# Development-only material. This does not untrack historical source files;
# the public release builder is the authority for the installable artifact.
tests/
test/
**/tests/
**/test/
benchmarks/
**/benchmarks/
fixtures/
**/fixtures/
research/
!/plugins/design/skills/research/
!/plugins/design/skills/research/SKILL.md
plans/
reports/
receipts/
evidence/
artifacts/
output/
tmp/
temp/
scratch/
coverage/
playwright-report/
test-results/
.pytest_cache/
.mypy_cache/
.ruff_cache/
**/*_test.py
**/test_*.py
**/*.test.js
**/*.test.ts
**/*.test.mjs
**/*.test.cjs
**/test-*.js
**/test-*.ts
**/test-*.mjs
**/test-*.cjs
**/test_*.sh
**/*-smoke.ts
**/*-smoke.mjs
**/*-smoke.cjs
**/*-benchmark.*
**/*benchmark*.*
.env.example
*.env.example

# ── Embedded engine (separate open-source repo) ──────────────────────────────
# Hephaestus is its own public repo (github.com/agentlas-ai/Hephaestus). It is
# embedded into the desktop build at PACKAGE time via electron-builder
# `extraResources` (filesystem copy, independent of git), NOT tracked here.
# This keeps the private desktop repo free of the engine source while still
# shipping the engine inside the .app. Engine must be present on disk to build.
/Hephaestus/
# Sibling agent-forge working tree (not shipped)
/Hephaestus_agent_forge/

# Root-level scratch / one-off scripts (not part of the app)
/clean_settings.py
/fix_settings.py
/make_collapsible.py
/test_wrap.py
/fix.js
/rewrite_panels.js
/rewrite_safe.js
/rewrite_settings.js
/renderer/app/(shell)/library/agents/transform.js
*.bak

# Embedded Startup Founder Studio package (bundled at build). Keep runtime/private
# data out, but track the sanitized launcher + built SPA so clean CI can package.
/studio-pack/.agentlas/
/studio-pack/.studio-runtime/
/studio-pack/**/__pycache__/
/studio-pack/**/*.pyc
/studio-pack/node_modules/
/studio-pack/web/node_modules/
/studio-pack/web/dist/generated/
/studio-pack/web/dist/studio-data.json
/studio-pack/web/public/generated/
/studio-pack/web/public/studio-data.json
/studio-pack/docs/dogfood/**/stitch/generated/

# Heavy T-rex sample/capture images — regenerated locally, never shipped
renderer/public/trex-samples/

# >>> agentlas local project state >>>
.agentlas/
.agentlas/project-soul-memory.md
.agentlas/sitemap.json
.agentlas/feature-map.json
.agentlas/memory-map.json
.agentlas/memory-tickets.jsonl
.agentlas/vault-references.json
.agentlas/local-credentials.map.json
.agentlas/activation.json
.agentlas/skill-registry.json
.agentlas/skill-trials.jsonl
.agentlas/curator-decisions.jsonl
.agentlas/code-map/
.agentlas/ontology-runtime.json
.agentlas/ontology-sources.json
.agentlas/ontology-inbox/
.agentlas/ontology-runtime.sqlite*
.agentlas/career-graph.json
.agentlas/career-graph-sources.json
.agentlas/career-graph-inbox/
.agentlas/career-graph.sqlite*
.agentlas/experience-relations.jsonl
.agentlas/stormbreaker/
.agentlas/pipeline/
.agentlas/*.lock
.agentlas/*.sqlite*
.agentlas/*.jsonl
.env
.env.*
!.env.example
.env.local
signing/*
!signing/README.md
credentials/*
!credentials/README.md
# <<< agentlas local project state <<<

# Agentlas local credentials and signing material never enter public commits.
.env
.env.local
.env.*.local
._*
signing/
credentials/
.agentlas/ontology-runtime.sqlite*
.agentlas/career-graph.sqlite*
.agentlas/experience-relations.jsonl*
.agentlas/.experience-relations.jsonl.*

# Rebuilt from native/macos/AgentlasInputDriver.swift for each macOS package.
/build-resources/native/macos/agentlas-input-driver
/build-resources/native/macos/.build-*

# Fetched CPython runtime (scripts/fetch-python-runtime.mjs writes the tree +
# manifest deterministically from its pinned version). It is a build cache, not
# source; only .gitkeep is tracked so the directory exists for the fetcher.
/build-resources/python-runtime/*
!/build-resources/python-runtime/.gitkeep

# Official Node.js runtime fetched only for the Windows no-Node CLI bootstrap.
# Keep the immutable fetcher/manifest contract in git, never the large runtime tree.
/build-resources/node-runtime/*
!/build-resources/node-runtime/.gitkeep
/build-resources/browser-runtime/*
!/build-resources/browser-runtime/.gitkeep

# Local visual QA evidence is never source or release material.
/.artifacts/
/.sei/
/scripts/validate-automation-session-clone.cjs
/scripts/validate-task-participant-repair-clone.cjs
/scripts/test-run-event-task-participants.cjs

# Agentlas One local QA harnesses and generated context maps. They are kept on
# disk for the live audit but are not product or release inputs.
agentlas-context-map.json
scripts/graph-runtime-permission-contract.cjs
scripts/one-*-contract.*
scripts/one-real-chat-qa-report.cjs
scripts/one-session-qa-scenarios.cjs
scripts/ontology-retirement-contract.mjs
scripts/qa-one-decision-receipt-freshness.cjs

# Tests are local-only (owner decision, 2026-07-26). CI builds, signs, and
# publishes; it never runs a suite. Run them here before pushing:
#   npm run test:<name>   /   electron scripts/test-<name>.cjs
# Release machinery stays tracked: scripts/*.mjs, scripts/release-*.mjs.
scripts/test-*.cjs
scripts/verify-*.cjs
# 점으로 시작하는 scripts/ 파일은 **한 번 쓰고 버리는 시험 하네스**다(캠페인 드라이버 등).
# 이 저장소는 공개다. 누군가 blanket add 를 하면 하네스가 그대로 공개되는데, 하네스는
# 사용자가 설치·실행하는 데 필요한 것이 아니다. 실측 2026-08-20: 이런 파일이 32개
# 추적 밖에 쌓여 있었고, 무시 규칙은 하나도 걸려 있지 않았다.
scripts/.*
scripts/lib/mock-*.cjs
scripts/fixtures/
# Exceptions: these two are packaging machinery, not suites. package-mac.sh and
# the cross-platform build run them against the built artifact, so CI must have
# them or packaging fails with file-not-found.
!scripts/verify-packaged-workforce-runtime.cjs
# The Windows/Linux updater E2E is the one check that cannot run on this Mac, so
# it stays in CI and the runner sparse-checks this exact file out of the release
# commit. It drives the built installer, not product internals.
!scripts/verify-packaged-updater-install-e2e.cjs

# session scratch/demo (never publish)
scripts/capture-x-graph.cjs
scripts/demo/

# Local QA drivers for the Build surface. These pilot the running app over CDP
# with a test account and are worthless to anyone else — they name this
# machine's scratch paths and grants.
/qa-*.mjs
/renderer/qa-*.mjs
/run-queue.sh

# Generated from the pinned maplibre-gl package before renderer dev/build.
/renderer/public/vendor/maplibre-gl/

# Generated from the pinned File Viewer dependency graph before renderer dev/build.
/renderer/public/file-viewer/

# Apple HIG page text is Apple's copyright and this repo is public — the design
# plugin keeps it as a local cache only (refill: node scripts/fetch-apple-hig.mjs).
plugins/design/references/apple-hig/.cache/

# 이 기계에서만 도는 검증·도구. 폴더째 무시한다 — 파일마다 규칙을 늘리지 않는다.
scripts/local/

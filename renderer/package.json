{
  "name": "agentlas-desktop-renderer",
  "version": "0.0.3",
  "private": true,
  "scripts": {
    "dev": "next dev -p 3100",
    "build": "next build",
    "lint": "next lint"
  },
  "comment": "React/Next는 루트(../package.json)에 설치된 단일 트리만 사용. 여기서 중복 install되면 worker가 React를 두 곳에서 로드해 useContext null 에러 발생."
}
